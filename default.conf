server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }
    
    # Database credentials
    set $db_user 'admin';
    set $db_pass 'SuperSecret123';
    
    # API key
    set $api_key 'API_KEY_ABC123';
}
